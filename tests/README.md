# Tests

Khu vực test tích hợp và end-to-end do Thành viên 4 sở hữu. Unit test của từng module đặt gần package tương ứng:

- Extension: `extension/src/tests/`.
- Khung Local Service: `local-rag-service/tests/service/`.
- AI/RAG: `local-rag-service/tests/rag/`.
- E2E toàn hệ thống: `tests/e2e/`.

